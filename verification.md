# verification.md

## 목적

이 저장소는 코드가 아니라 문서 구조 자체가 산출물이다. 이 문서는 그 문서 구조를 어떻게 확인할지, 그리고 에이전트가 구조를 고치다가 무한 루프에 빠지지 않게 막을 기준을 정의한다.

## Computational 체크 (빠르고 결정적)

- `grep -r`로 변경한 파일명/경로를 다른 문서가 참조하는지 확인 (예: `verification.md`, `docs/modules/harness-engineering.md`)
- `AGENTS.md`가 50줄 안팎인지 줄 수 확인
- `README.md`의 "템플릿 복사 위치" 표와 실제 `templates/` 디렉터리 목록이 일치하는지 확인
- 실행 시점: 구조를 바꾸는 모든 작업 직후

## Inferential 체크 (느리고 의미 기반)

- 새 에이전트 입장에서 `AGENTS.md`와 `README.md`만 읽고 구조를 이해할 수 있는지 검토
- 추가한 절(예: "Harness Engineering 관점")이 기존 절과 역할이 겹치지 않는지 검토
- 자동화 여부: 사람 또는 리뷰어가 수동 확인

## 게이팅 순서 (Keep quality left)

- 통합 전에 끝낼 체크: grep 기반 상호 참조 확인, 줄 수 확인
- 통합 후에만 돌리는 체크: 전체 문서를 처음부터 읽는 의미 검토

## 무한 루프 방지

문서 구조를 고치는 작업은 "한 곳을 고치면 다른 참조가 깨지는" 연쇄가 발생하기 쉽다. 아래 신호가 보이면 같은 시도를 더 반복하지 않고 멈춘 뒤 보고한다.

- 같은 파일을 3회 이상 다시 고치는데도 상호 참조 불일치가 해소되지 않는다.
- 번호를 다시 매기거나 표현을 다듬는 시도가 같은 결과로 되돌아온다(진전 없음).
- 두 문서 사이를 오간다. A 문서를 B 기준으로 고치면 B가 A 기준과 다시 어긋난다.

멈췄을 때는 어떤 파일까지 확인했는지와 어떤 불일치가 남았는지를 구분해서 보고한다.

- 최대 반복 횟수: 같은 파일에 대해 3회
- 시간/토큰 예산: 별도 budget 없음. 대신 반복 횟수로만 제한
- 멈춤 시 보고 형식: 변경한 파일 목록 + 확인된 사실 + 막힌 지점

## 반복되는 실수 처리 규칙

같은 종류의 실수(예: 템플릿 추가 후 README 표 갱신을 빠뜨림)가 두 번 이상 나오면, 지시문 추가보다 아래를 우선 검토한다.

- `docs/collab_protocol.md`의 "충돌 방지 규칙"에 체크리스트 항목 추가
- 해당 누락이 반복된 파일 쌍을 `docs/modules/*.md`의 "수정 시 주의"에 명시

어떤 장치로 옮겼는지 `project.md`의 "최근 결정"에 기록한다.

## 적용 범위 밖

이 저장소는 문서 구조만 다룬다. 실제 코드 빌드, 테스트 실행기, 도구 권한, observability는 이 저장소가 만들어지는 대상 프로젝트의 `verification.md`에서 다룬다.
