# AGENTS.md

## 역할

이 문서는 이 저장소에서 작업하는 모든 AI 에이전트가 따라야 하는 공통 최소 규칙이다.

문서에 `Codex`라고 적혀 있더라도 특정 제품명을 뜻하지 않는다. 실제 운영에서는 `GPT`, `ChatGPT`, `Claude Code` 같은 도구가 같은 규칙을 사용해도 된다.

## 작업 원칙

1. `AGENTS.md`에는 최소 규칙만 두고, 상세 설명은 다른 문서로 분리한다.
2. 템플릿을 추가하기 전에 실제 사용 흐름에서 빠진 문서가 무엇인지 먼저 확인한다.
3. 작업 범위는 작게 정의하고, 목적과 검증 방법을 분명히 적는다.
4. 검증하지 않은 내용은 완료로 적지 않는다.
5. 문서 구조가 바뀌면 관련 인덱스와 템플릿도 같이 갱신한다.
6. 읽기 순서는 `docs/collab_protocol.md`의 단일 기준을 따른다.
7. 문서가 길어지면 계속 덧붙이지 말고 역할별 문서로 분리한다.
8. 같은 실수가 반복되면 지시문 추가보다 `verification.md`의 검증 장치(테스트/린트/권한 축소)로 옮긴다.
9. 같은 시도를 반복해도 진전이 없으면(같은 에러, 같은 diff, 상태가 왔다갔다 함) 멈추고 막힌 지점을 보고한다. 무한 루프 방지 기준은 `verification.md`를 따른다.

## 책임 범위

- 문서 구조 유지
- 작업 상태 관리
- 상세 문서와 템플릿 정합성 점검
- 완료 기준 관리

## 크기 기준

- 이 파일은 가능하면 50줄 안팎을 유지한다.
- 한 문서가 약 150줄을 넘거나 서로 다른 목적 2개 이상을 담기 시작하면 분리를 검토한다.
- 작업 로그나 진행 메모가 누적되면 `tasks/*` 또는 별도 기록 문서로 이동한다.

## 참조 문서

- 구조 개요: `README.md`
- 작업 상태: `project.md`
- 협업 규칙: `docs/collab_protocol.md`
- 상세 템플릿: `docs/template.md`
- 검증/피드백 루프: `verification.md`
